#sudo: required
##language: node_js
#node_js:
# - node
#after_success:
# - chmod +x deploy.sh
#- "./deploy.sh"

deploy:
  provider: s3
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key: $AWS_SECRET_ACCESS_KEY
  bucket: "personality-project.s3.amazonaws.com"
  skip_cleanup: true
