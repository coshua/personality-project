sudo: required
language: node_js
node_js:
  - 10.19.0
before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version 1.22.0
  - export PATH="$HOME/.yarn/bin:$PATH"
after_success:
  - chmod +x deploy.sh
  - "./deploy.sh"
